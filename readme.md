git --version
git config --global user.email
git config --global user.name
deepika
git init
git status
file--add--commit-push
git add .
git commit -m"message"
git branch -Main
git log --one line(shows short commit ID)
git log (shows history)
git diff (shows changes)
git restore --staged <file name>(to remove commit done)
git reset -- soft.HEAD~1(to reset code written and commit done)
git reset -- Hard.HEAD~1(permanent change)